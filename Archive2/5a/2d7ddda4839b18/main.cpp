#include <iostream>#include <iterator>#include <algorithm>#include <map>#include <fstream>#include <string>      struct the_xml_doc{    struct DLNA    {        std::string isLocal;        std::string manufacturer;        std::string modelName;        std::string IPAddress;        std::string modelNo;    	std::string modelUrl;	    	std::string serialNo;                DLNA(std::string& input) //or std::vector<std::string>, or std::ifstream, depending on how you implement it        {};    };        std::vector<DLNA> DLNAs;        the_xml_doc(std::ifstream& input)    {};};int main(){    std::ifstream my_file("//Archive2//60//118f9d6d97b206//main.cpp");    the_xml_doc doc(my_file);    //the_xml_doc has all the data loaded at this point    return 0;}