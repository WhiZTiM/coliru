#include <iostream>#include <iterator>#include <algorithm>#include <map>#include <fstream>#include <string>class Level {private:    int size;    float** a_predefined_2d_array;public:    int GetSize();    int SetSize(int);    int DoSomething();    int Load(std::string, int);};int Level::GetSize() {   return size;}int Level::SetSize(int _size) {   size = _size;   return size;}int Level::Load(std::string str, int _size) {   SetSize(_size);             //works fine!   std::cout<<GetSize()<<std::endl;      //works fine!   //basically loads everything to initiliaze the program. The point is that the above calls work fine!   return GetSize();}//The problem is here!!!int Level::DoSomething() {   a_predefined_2d_array = new float*[GetSize()]; //SEG FAULTS!!!   std::cout<<"size = "<<GetSize()<<std::endl; //SEG FAULTS!!!   return GetSize();}int main(){    Level myself;    myself.SetSize(5);    myself.DoSomething();    return 0;}