#include <string>

int main() {
    int array_length = 0;
    std::string city_name;
    
    for(int x = 0; x < array_length; x++)
    { if(city_name[x] == 'a' || 'e')
      city_name[x] = ' ';}   
}
