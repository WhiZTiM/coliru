#include <stdio.h>
int main()
{
    int sum = 0;
    for (int i = 50; i <>
int factorial(int x) {
    return !x ? 1 : x * factorial(x – 1);
}
int main()
{
    int n; while( scanf("%d", &n) == 1)
    printf("%d\n", factorial (n));
} 